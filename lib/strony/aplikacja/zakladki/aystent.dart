import 'package:flutter/material.dart';

class Aystent extends StatelessWidget {
  const Aystent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
